div.row
	h3 Game Builder
	p Drag and drop players and positions to get things organized. We've given you one inning to start, but add all you like. 
div.row
	button.btn.btn-success(ng-click="addInning()") Add Inning
.row.gamegrid
	.col-sm-3
		.tools &nbsp;
		ul(ng-model="players",ui-sortable)
			li.list-unstyled.draggable-item(ng-repeat="p in players") {{p.name}}
				a.delete(ng-click="removePlayer($index)",title="Remove") x
	.col-sm-1(ng-repeat="i in innings")
		.tools 
			a(ng-click="shuffle($index)") Shuffle
		ul(ng-model="i.positions",ui-sortable)
			li.list-unstyled.draggable-item(ng-repeat="p in i.positions") {{p.abbr}}
.row
	h4 Not Playing
	ul
		li.list-inline(ng-repeat="p in absent") Out: {{ p.name }}